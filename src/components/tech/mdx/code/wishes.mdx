```ts
<MdxWishes />
  <div className="space-y-5">
    <div className="max-w-2xl space-y-5">
      <div className="grid w-full gap-1.5">
        <Label htmlFor="email">Email</Label>
        <Input type="email" id="email" placeholder="Email" {...form.register('name')} />
      </div>
      <div className="grid w-full gap-1.5">
        <Label htmlFor="message">Wish</Label>
        <Textarea placeholder="Type your message here." {...form.register('wish')} />
      </div>
      <div>
        <Button>Save</Button>
      </div>
      <div className="space-y-3 max-h-96 overflow-y-auto overflow-scroll bg-slate-900 p-5 rounded-lg">
        {DUMMY_WISHES.map((wish, index) => (
          <div key={index} className="flex flex-row justify-between gap-2">
            <div>
              <div className="text-base font-bold">{wish.name}</div>
              <div className="text-base">{wish.wish}</div>
            </div>
            <div className="text-xs text-slate-400">{wish.created_at}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
```
